import { ServiceProviderDialogData } from './service-provider-dialog-data';

describe('ServiceProviderDialogData', () => {
  it('should create an instance', () => {
    expect(new ServiceProviderDialogData()).toBeTruthy();
  });
});
