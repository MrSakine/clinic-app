import { CashierDialogData } from './cashier-dialog-data';

describe('CashierDialogData', () => {
  it('should create an instance', () => {
    expect(new CashierDialogData()).toBeTruthy();
  });
});
