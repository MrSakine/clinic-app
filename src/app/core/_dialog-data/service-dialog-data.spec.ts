import { ServiceDialogData } from './service-dialog-data';

describe('ServiceDialogData', () => {
  it('should create an instance', () => {
    expect(new ServiceDialogData()).toBeTruthy();
  });
});
